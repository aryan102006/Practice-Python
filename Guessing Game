#Generate a random number between 1 and 9 (including 1 and 9). Ask the user to
#guess the number, then tell them whether they guessed too low, too high, or
#exactly right. 

import random
a=0
num = random.randint(1,100)

while a != num:
	a = int(raw_input("Enter a number between 1 & 100: "))
	if a > num:
		print("too high!")
		continue
	if a < num:
		print("too low!")
		continue	
	if a == num:
		print("exactly right!")
		break
